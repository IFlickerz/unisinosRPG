#include "include/Game.h"
#include <iostream>

int main() {

    // Initializing the game engine
    Game game;

    // Starting exec
    game.start();

    std::cout << "Fim" << std::endl;

    return 0;
}